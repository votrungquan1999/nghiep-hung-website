/**
 * Common shared types used across the application
 * Contains reusable type definitions that are not specific to any single feature
 */

/**
 * Multilingual text content for internationalization
 * Used for content that needs to be displayed in both English and Vietnamese
 */
export interface MultilingualText {
	en: string;
	vi: string;
}
